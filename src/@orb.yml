version: 2.1

description: >
  This ord will be used in the Cyber4All organization to globalize config.yml files
  The goal is to have the config.yml file in every repo to just be short and use this orb to complete
  all of its tasks.

# Orbs that will be used in different jobs
orbs:
  aws-cli: circleci/aws-cli@1.2.1
  docker: circleci/docker@1.4.0
  node: circleci/node@1.1.6
